# VideoAgent 配置操作手册


## 1. 配置文件在哪里

交付的 `video_agent.exe` 第一次启动后，会在当前 Windows 用户桌面创建：

```text
桌面\VideoAgent\
├─ config.yaml                 # 实际生效的配置文件
├─ config\
│  ├─ iectp_rsa_private.pem    # 私钥：仅保存在本机
│  └─ iectp_rsa_public.pem     # 公钥：按平台要求登记
├─ recordings\                # 每个任务的录制文件和诊断信息
└─ logs\                      # 预留日志目录（运行日志默认输出到控制台）
```

首次部署可先在 EXE 所在目录运行：

```powershell
.\video_agent.exe --init-only
```

它只创建运行目录和基础文件，不会领取任务、打开会议客户端或启动录制。

## 2. 首次配置清单

开始前确认：

- OBS 已安装，并在“工具 → WebSocket 服务器设置”中启用 WebSocket；端口和密码与 `obs` 配置一致。
- 需要使用的平台客户端已安装。腾讯会议、钉钉、觅讯需要能正常登录；微信视频号需要微信长期保持登录。
- 管理员已提供平台认证和 MinIO 上传配置。
- 平台若下发加密的登录账号或密码，录制机的 RSA 公钥已按管理员要求登记。

软件安装在常见路径时，`executable_path` 可留空，Agent 会自动查找。安装在非标准位置、自动查找失败时，手动填写完整 EXE 路径。

## 3. 配置块说明

### `agent`：运行节奏

| 字段 | 作用 | 建议 |
| --- | --- | --- |
| `agent_id` | 录制机标识，供平台定制接入使用 | 保留管理员提供的值 |
| `poll_interval_seconds` | 无任务或任务结束后的轮询间隔 | 默认 `15`；不要设得过小 |
| `prepare_before_minutes` | 在计划开始前多久开始准备客户端 | 默认 `5` 分钟 |
| `max_retries` | 预留配置项 | 当前版本不用于任务自动重试，无需调整 |
| `close_apps_after_task` | 任务结束后关闭 Agent 启动的会议客户端和 OBS | 通常保持 `true`；微信 Provider 不会退出微信 |

### `platform`：任务平台与认证

| 字段 | 作用 |
| --- | --- |
| `base_url` | 任务平台地址，例如 `https://platform.example.com/` |
| `api_token` | 可选固定 token；为空时使用下面的应用认证换取 token |
| `app_id` / `app_secret` / `tenant_id` | 获取 token 的应用和租户认证信息 |
| `token_path` | 获取 token 接口路径 |
| `pending_tasks_path` | 拉取待录制任务接口路径 |
| `start_callback_path` | OBS 开始录制后的回调接口路径 |
| `complete_callback_path` | 成功、上传完成或失败时的回调接口路径 |
| `headers` | 平台要求附带的业务请求头 |
| `provider_aliases` | 将平台的 `livePlatform` 名称映射到 Provider 名称 |
| `timeout_seconds` | 单次平台 HTTP 请求超时，单位秒；默认 `20` |


### RSA 密钥配置

`rsa_private_key_path`、`rsa_public_key_path`、`rsa_generate_if_missing` 与 `rsa_decrypt_passwords` 位于 `platform` 下。

- 正常 EXE 运行时，密钥固定使用 `桌面\VideoAgent\config\` 内的 PEM 文件；不需要手工改路径。
- 私钥用于解密平台下发的敏感账号、密码等字段。
- 公钥可按平台要求提供给管理员登记；后端使用它加密敏感字段。
- 不要随意删除或替换私钥。替换后，原来按旧公钥加密的任务将无法解密。
- `rsa_decrypt_passwords` 应保持 `true`，除非平台明确说明下发的是明文且允许关闭。

### `obs`：录制和 WebSocket

```yaml
obs:
  executable_path: "D:\\Program Files\\obs-studio\\bin\\64bit\\obs64.exe"
  websocket_host: "127.0.0.1"
  websocket_port: 4455
  websocket_password: "由管理员提供"
  startup_timeout_seconds: 90
  mute_microphone_during_recording: true
```

| 字段 | 说明 |
| --- | --- |
| `executable_path` | OBS 非标准安装路径；标准路径可留空 |
| `websocket_host` / `websocket_port` | 通常为 `127.0.0.1` 与 `4455`，必须和 OBS WebSocket 设置一致 |
| `websocket_password` | OBS WebSocket 密码；属于敏感信息 |
| `startup_timeout_seconds` | OBS 冷启动、连接 WebSocket、开始/停止录制的最长等待时间，当前建议 `90` 秒 |
| `mute_microphone_during_recording` | 建议保持 `true`；录制时静音 OBS 的麦克风/Aux，结束后恢复任务开始前的静音状态，不影响会议或直播应用声音 |
| `recordings_dir` | EXE 运行时固定改为 `桌面\VideoAgent\recordings`，无需手工填写 |

### `upload` 与 `minio`：上传录制文件

| 配置块 | 字段 | 作用 |
| --- | --- | --- |
| `upload` | `part_size_bytes` | 分片上传预留参数；当前 MinIO 直传通常保持默认值 |
| `upload` | `retry_count` | 分片上传预留重试次数，通常保持默认值 |
| `minio` | `endpoint` | MinIO 服务地址 |
| `minio` | `access_key` / `secret_key` | MinIO 访问密钥，属于敏感信息 |
| `minio` | `bucket_name` | 存储桶名称 |
| `minio` | `secure` | MinIO 是否使用 HTTPS；必须与实际服务一致 |
| `minio` | `object_prefix` | 上传对象路径前缀，通常保持管理员提供的值 |

MinIO 配置不完整时，录制可能完成，但上传会失败。

## 4. `providers`：各平台配置

### 腾讯会议 `tencent_meeting`

- 前置条件：客户端可正常打开；使用任务账号密码登录时，不应停留在验证码、扫码或协议确认页面。
- 常用项：`executable_path`、`window_title_regex`、`login_retry_count`、`join_retry_count`、`meeting_end_poll_seconds`、`capture_health_check_seconds`。
- `capture_health_check_seconds` 默认 `5`，会在录制开始后检查 OBS 捕获画面是否为黑屏。
- 仅在客户端版本导致原有定位失效时，才由维护人员调整登录、设置或协议弹窗相关的高级 `*_click_ratio` 字段。

### 钉钉 `dingtalk`

- 前置条件：客户端可启动，任务账号可密码登录；验证码、扫码或设备验证需要人工处理。
- 常用项：`executable_path`、`startup_timeout_seconds`、`login_retry_count`、`join_retry_count`、`password_prompt_timeout_seconds`、`meeting_end_poll_seconds`。
- `home_join_webview_anchor` 是“会议首页 → 加入会议”WebView 内的相对锚点。仅当客户端改版、实际点击错位时，用检查结果校准；不要随意修改。
- `window_title_regex`、`meeting_nav_index`、`agreement_click_ratio` 等属于高级兼容配置，只由维护人员调整。

### 觅讯 `mixlink`

- 前置条件：觅讯客户端可启动、可登录并可手工加入会议。
- 常用项：`executable_path`、`startup_timeout_seconds`、`login_retry_count`、`join_retry_count`、`password_prompt_timeout_seconds`、`meeting_end_poll_seconds`。
- 若示例中存在其他机器的路径，必须改成当前录制机路径，或清空后让 Agent 自动查找。

### 抖音直播 `douyin_live`

- 前置条件：已安装 Chrome；任务必须提供合法的抖音直播间 HTTPS URL。
- 常用项：`executable_path`、`profile_dir`、`debugging_port`、`playback_timeout_seconds`、`end_poll_seconds`、`end_delay_seconds`、`capture_health_check_seconds`。
- `profile_dir` 为空时使用 Agent 独立的 Chrome 配置目录，不使用用户平时的 Chrome 登录资料。
- `debugging_port` 默认 `9229`；同一台电脑同时运行多个抖音录制实例时必须使用不同端口。

### 微信视频号 `wechat_live`

- 前置条件：微信客户端已登录且 Windows 桌面保持解锁；任务使用“搜索口令”，口令对应已关注视频号的显示昵称。
- Agent 不填写微信账号密码，也不会在任务结束后退出微信。
- 常用项：`executable_path`、`window_title_regex`、`startup_timeout_seconds`、`navigation_timeout_seconds`、`page_transition_timeout_seconds`、`refresh_poll_seconds`、`follow_scroll_max_pages`、`capture_health_check_seconds`。
- 找不到直播时会按 `refresh_poll_seconds` 刷新目标主页，直到计划结束；到期仍未开播会失败。
- `video_channel_click_ratio`、`profile_click_anchor`、`follow_click_anchor` 及其 `*_ratio` 回退值用于 UI 校准。仅在相应图标无法用控件定位且检查工具确认后修改。

## 5. 不要随意修改的项目

- `platform` 中的密钥、接口路径、业务 Header 与 `provider_aliases`。
- `minio` 中的访问密钥、存储桶和路径前缀。
- RSA 私钥、公钥与 RSA 路径配置。
- 微信、钉钉和腾讯会议的高级锚点、比例坐标、窗口匹配字段。
- OBS WebSocket 端口和密码；修改后必须同时修改 OBS 软件设置。

## 6. 常见问题

| 现象                                 | 优先检查                                                                  |
|------------------------------------|-----------------------------------------------------------------------|
| `obs_websocket_failed` 或连接被拒绝      | OBS 是否已安装并启用 WebSocket；端口、密码是否一致；`startup_timeout_seconds` 是否至少为 `90` |
| `OBS executable not found` 或客户端未找到 | 先检查软件是否安装；非标准安装位置填写对应 `executable_path`                               |
| 获取 token 失败                        | 核对 `base_url`、应用认证、租户和业务 Header；联系平台管理员                               |
| 录制黑屏                               | 目标会议/直播窗口是否可见；OBS 是否允许窗口捕获；检查任务目录下 `diagnostics` 中的截图                 |
| 上传失败                               | 核对 MinIO 地址、密钥、bucket、HTTPS 设置和网络连通性                                  |
| 微信提示未登录或无法进入视频号                    | 在同一 Windows 用户、已解锁桌面中手工确认微信已登录；不要让系统锁屏                                |
| 修改配置后没有生效                          | 确认编辑的是 `桌面\VideoAgent\config.yaml`；重启 EXE；检查 YAML 缩进和引号               |

## 7. 安全要求

以下内容不得写入文档、截图、群聊或工单公开内容：

- `app_secret`、`api_token`、业务请求 Header 中的身份信息；
- MinIO `access_key`、`secret_key`；
- OBS `websocket_password`；
- `iectp_rsa_private.pem` 的完整内容；
- 平台下发的会议密码、客户端登录密码。
